[phases.setup]
nixPkgs = ["git-lfs"]

[phases.install]
cmds = [
  "git lfs pull",
  "pip install -r requirements.txt"
]

[start]
cmd = "streamlit run FRONT/main.py --server.port $PORT --server.address 0.0.0.0"